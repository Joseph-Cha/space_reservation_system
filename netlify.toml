[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA 라우팅을 위한 리다이렉트 설정
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 환경 변수 (필요시 추가)
[build.environment]
  NODE_VERSION = "18"
